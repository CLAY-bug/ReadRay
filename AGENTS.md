# ReadRay 协作说明

ReadRay 是一个单人开发、Windows 优先的本地英语学习 Agent。项目方向、技术栈和阶段计划以 `docs/DEVELOPMENT_PLAN.md` 为准。

## 启动流程

每次开始处理本仓库任务时：

1. 先阅读本文件。
2. 再阅读 `docs/RESOURCE_MAP.yml`。
3. 根据资源地图判断还需要读取哪些文件。
4. 如果任务涉及项目方向、架构、范围或路线，再阅读 `docs/DEVELOPMENT_PLAN.md`。
5. 如果任务涉及继续开发或恢复上下文，再阅读 `docs/HANDOFF.md`。
6. 默认不要全量扫描整个仓库。

## 必须遵守的规则

- 所有项目文档必须使用中文。
- `AGENTS.md` 只记录会改变 Codex 行为的规则；背景信息、阶段计划和技术细节放到 `docs/`。
- 当本地文档已经记录决策时，优先依据本地文档，不要从聊天历史重建结论。
- 进行重要实现选择前，先说明判断依据。
- 不确定时，先检查本地文件，不要凭印象猜测。
- 涉及代码修改时，在可行范围内完成实现和验证；无法验证时说明原因。
- 只有任务产生交接价值时才更新 `docs/HANDOFF.md`；小型措辞、格式或不影响后续恢复的修改不记录。
- 不得在非空项目根目录使用带覆盖或强制语义的初始化命令，例如 `--force`、`overwrite`、`clean init`；如确需使用，必须先确认 Git 可用或完成备份，并说明会影响哪些文件。

## 写代码规则

- 写新代码前，先定位现有扩展点和相邻实现；能复用或参数化现有代码时，不新建同类第二套实现。
- 新函数、新类、新模块必须职责单一；不要创建“万能 util”或把无关逻辑塞进入口文件。
- 只改完成当前任务所必需的装配点；不得顺手重排目录、重命名无关文件或做无关重构。
- 不新增依赖，除非先说明必要性、替代方案，以及它对打包和运行环境的影响。
- 完成代码修改后，必须说明改了哪些文件、如何验证、还有哪些未验证风险。
