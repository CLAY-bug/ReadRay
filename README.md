# ReadRay

ReadRay 是一个 Windows 优先的本地英语学习 Agent。

当前仓库处于阶段一：打通 Tauri + React + TypeScript 的桌面应用基础能力。

## 开发命令

```powershell
pnpm install
pnpm build
pnpm tauri dev
```

Tauri 开发需要本机已安装 Rust、Cargo、Microsoft C++ Build Tools 和 WebView2。
